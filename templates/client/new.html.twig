{% extends 'base.html.twig' %}

{% block title %}Créer un nouveau client{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow rounded-4 p-4">
        <h1 class="text-center mb-4">Créer un nouveau client</h1>

        {{ include('client/_form.html.twig') }}

        <div class="mt-3">
            <a href="{{ path('app_client_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
        </div>
    </div>
</div>
{% endblock %}
