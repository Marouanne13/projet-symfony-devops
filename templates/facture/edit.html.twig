{% extends 'base.html.twig' %}

{% block title %}Modifier la facture{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow rounded-4 p-4">
        <h1 class="text-center mb-4">Modifier la facture</h1>

        {{ include('facture/_form.html.twig', {'button_label': '🔄 Mettre à jour'}) }}

        <div class="d-flex justify-content-between mt-4">
            <a href="{{ path('app_facture_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
            {{ include('facture/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
